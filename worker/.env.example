TWOCAPTCHA_API_KEY=
HEADLESS=false

# only needed to run server.ts (the web frontend's backend) - a plain
# `npm run dev` CLI run doesn't touch GitHub at all
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
PORT=3001
